name: panstwa_miasta_design
description: Shared design tokens and Flutter theme for the Panstwa Miasta game.
version: 0.2.0
publish_to: none

repository: https://github.com/PawelWielga/panstwa-miasta-design

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: '>=3.32.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
